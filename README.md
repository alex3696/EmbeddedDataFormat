# EDF (Embedded Data Format)

## Оглавление

1. [Введение и мотивация](#введение-и-мотивация)
2. [Область применения](#область-применения)
3. [Ключевые термины](#ключевые-термины)
4. [Главные принципы и ограничения](#главные-принципы-и-ограничения)
5. [Структура файла и блоков](#структура-файла-и-блоков)
   - [Конфигурационный блок](#конфигурационный-блок)
   - [Блок схемы (Schema–block)](#блок-схемы-schema-block)
   - [Блок данных (Data–block)](#блок-данных-data-block)
      - [Сериализация данных в бинарном виде](#Сериализация-данных-в-бинарном-виде)
      - [Сериализация данных в текстовый виде](#Сериализация-данных-в-текстовый-виде)
6. [Десериализация](#Десериализация)
7. [Чтение множественных схем и записей](#чтение-множественных-схем-и-записей)
8. [API: функции работы с EDF](#api-функции-работы-с-edf)
9. [Коды ошибок](#коды-ошибок)
10. [Примеры](#примеры)
11. [Практические ограничения, замечания и FAQ](#практические-ограничения-замечания-и-faq)

---

## 1. Введение и мотивация

**EDF** — бинарный блочный формат для компактного хранения и обмена структурированными данными в embedded-системах с ограниченными ресурсами.
Предназначен для хранения, записи и передачи структурированных бинарных (для удобства просмотра имеет текстовое представление) данных во flash/EEPROM микроконтроллеров, ведения логов и обмена между устройствами и ПК. Формат создавался как альтернатива LAS и CSV, но с явным встроенным описанием схем (schema-on-data), поддержкой вложенных структур, хранением метаданных, надёжным восстановлением данных и минимальными требованиями к памяти.
Отличие от LAS, CSV и JSON:
- Явная схема внутри данных (schema-on-data)
- Простота парсинга без внешнего описания структуры
- Лёгкое восстановление при потере блока (даже без кэша схем на устройстве)
- Отсутствие динамического выделения памяти

## Область применения

- Формат обмена/архивации для автономных и маломощных систем
- Хранение табличных данных во flash/EEPROM
- Обмен структурированными данными между embedded-устройствами и ПК
- Логирование (телеметрия устройств, события)

## Ключевые термины

| Термин      | Описание                                |
|-------------|-----------------------------------------|
| **Блок**    | Минимальная транспортная единица файла  |
| **Запись**  | Полностью сериализованный объект данных |
| **Примитив**| Базовый тип данных (например, Int32)    |
| **Схема**   | Описание структуры данных (`EdfSchema_t`)|

---

## Главные принципы и ограничения

- **Блочность:** Всё хранится и передаётся блоками. Размер блока: 256…4096 байт.
- **Контроль целостности (CRC16 Modbus):** В бинарном режиме, по блоку.
- **Append-only:** Запись только с добавлением, идеально для флеш-памяти/журналов.
- **Восстановление:** последовательности при повреждении блоков
- **Метаданные — внутри:** Схема данных сериализуется до данных.
- **Расширяемость:** — пользовательские структуры и типы
- **Без malloc:** Только статические и линейные аллокаторы.
- **только Little Endian-архитектура.** - предназначено только для Little-endian платформ, т.к. современных big-endian - не существует
- **Строки:** Только UTF-8, максимум 255 байт (обрезка без предупреждения).
- **Структуры и вложенность ограничены по размеру блока — слишком сложные не сериализуются.**
- **Нет поддержки произвольной выборки данных разных схем:** Data может относиться только к последней активной схеме (см. [Чтение множественных схем и записей](#чтение-множественных-схем-и-записей)).
- **Простая и компактная реализация, чтобы было легко портировать на любой язык


**Для фукционирования требуется реализовать для своей платформы**
- FileStreamImpl.c, FileStreamOpen основная функция для определения,
    которая инициализирует имплементацию StreamFnImpl_t: Write, Read, WriteFmt, Close, Seek
- MbCrc.c, MbCrc16acc - табличный расчёт контрольной суммы модбас

---

## Структура файла и блоков

### Таблица типов блоков

| Название   | Код (dec/hex) | Символ | Текстовый вид | Назначение             |
|------------|---------------|--------|---------------|------------------------|
| Конфиг     | 126 / 0x7E    | `~`    | `<~ ... >`    | Конфигурация файла     |
| Схема      | 63  / 0x3F    | `?`    | `<? ... >`    | Определение схемы      |
| Данные     | 61  / 0x3D    | `=`    | `<= ... >`    | Пользовательские данные|

---

### Общая структура блока (EdfBlock_t)

| Поле    | Размер    | Описание                               |
|---------|-----------|----------------------------------------|
| Type    | 1         | Тип блока                              |
| Len     | 2         | Длина Content                          |
| Content | [Len]     | Полезная нагрузка                      |
| CRC     | 2         | CRC16 (Modbus) от (Type+Len+Content)   |

- Каждый блок завершается CRC16.
> **Примечание**: В текстовом режиме поле CRC отсутствует, принципиально не требуется, блок завершается символом `>`.

---

### Конфигурационный блок - EdfConfig_t

**Первый блок в EDF-файле, задаёт глобальные параметры.**

| Поле      | Тип      | Описание                 |
|-----------|----------|--------------------------|
| VersMajor | uint8_t  | Главная версия формата   |
| VersMinor | uint8_t  | Минорная версия формата  |
| Encoding  | uint16_t | Кодировка (только 65001) |
| Blocksize | uint16_t | Размер блока (256–4096)  |
| Reserved  | uint16_t | Зарезервировано (0)      |
| Flags     | uint32_t | Зарезервировано (0)      |

**Пример:** бинарный вид

| Type | Len   | Content                            | CRC  |
|------|-------|------------------------------------|------|
| 7E   | 0C 00 | 00 03 E9 FD 00 02 00 00 00 00 00 00|16 FA |


**Пример:** текстовый вид
```
<~ {version=1.0; bs=512; encoding=65001; flags=0;} >
```

---

### Блок схемы (Schema–block)

**Содержит описание структуры пользовательских данных. Схема сериализуется полностью в один блок.**
- **Размер схемы и записи**: не может превышать Blocksize.

#### Формат EdfSchema_t (бинарный):

| Поле       | Размер (байт)   | Описание                                            |
|------------|-----------------|-----------------------------------------------------|
| Id         | 2               | Идентификатор схемы (0…65535)                       |
| Name       | 1+N             | Имя схемы (byte length + bytes, ≤255)               |
| Desc       | 1+M             | Описание схемы (опционально, как строка)            |
| Type       | переменный      | Cостав поля (см. EdfType_t, рекурсивно)             |

#### Формат EdfType_t:

| Поле        | Размер (байт)    | Описание                                                        |
|-------------|------------------|-----------------------------------------------------------------|
| Type        | 1                | Примитивный или составной тип , см. PoType                      |
| DimsCount   | 1                | Кол-во измерений массива (0–255)                                |
| Dims        | DimsCount×2      | Размеры измерений (uint16_t)                                    |
| Name        | 1+N              | Имя типа/поля (строка)                                          |
| FieldsCount | 1 or отсутствует | Число полей (Struct) **Только если Type==Struct и FieldsCount>0**|
| Fields      | переменный       | (FieldsCount × EdfType_t)                                       |

**Примечания:**
- FieldsCount есть ТОЛЬКО если Type==Struct и FieldsCount>0
- Поля структуры хранятся строго в порядке объявления
- Максимум 255 полей в одной структуре (uint8_t)
- Общее число примитивов в структуре (включая вложенные и массивы) ≤ 65535

#### Полная таблица примитивных типов PoType

| Название | Код | Размер (байт) | C-тип      | Примечание                                       |
|----------|-----|---------------|------------|--------------------------------------------------|
| Struct   | 0   | –             | –          | Вложенная структура                              |
| Char     | 1   | 1             | char[N]    | Фиксированный массив ≤255 байт                   |
| Int8     | 2   | 1             | int8_t     |                                                  |
| UInt8    | 3   | 1             | uint8_t    |                                                  |
| Int16    | 4   | 2             | int16_t    |                                                  |
| UInt16   | 5   | 2             | uint16_t   |                                                  |
| Int32    | 6   | 4             | int32_t    |                                                  |
| UInt32   | 7   | 4             | uint32_t   |                                                  |
| Int64    | 8   | 8             | int64_t    |                                                  |
| UInt64   | 9   | 8             | uint64_t   |                                                  |
| Half     | 10  | 2             | uint16_t   | 16-bit float (IEEE 754-2008)                     |
| Single   | 11  | 4             | float      |                                                  |
| Double   | 12  | 8             | double     |                                                  |
| String   | 13  | 1+N           | char*      | Len (1 байт) + Data (≤255 байт, без '\0' в конце)|

- Char — используется для строк фиксированной длины (например, char[10]). В бинарном виде хранится как сырые байты. В текстовом виде заключается в кавычки.
Пустые элементы в конце дополняются '\0'.
- String — динамическая строка, в бинарном виде имеет префикс длины (1 байт).
- Строки обрезаются до 255 байт (без предупреждения)

**Пример текстового блока схемы:**
```
<~ {version=1.0; bs=512; encoding=65001; flags=0;} >

<? {0;"SensorData"} Struct {
  UInt32 "timestamp";
  Single "value";
  String "unit";
} >
```
**Пример бинарного блока схемы:**
```
Id:       00 00
Name:     0A 'S' 'e' 'n' 's' 'o' 'r' 'D' 'a' 't' 'a'
Desc:     00
Type:     00 // Struct
Dims:     00
Name:     06 'S' 'e' 'n' 's' 'o' 'r'
Fields:   03
    Field0: 07 00 09 't' 'i' 'm' 'e' 's' 't' 'a' 'm' 'p'
    Field1: 0B 00 05 'v' 'a' 'l' 'u' 'e'
    Field2: 0D 00 04 'u' 'n' 'i' 't'
```

> **Важно:** После записи блока схемы состояние контекста автоматически сбрасывается для записи новых данных:
> - `SchId` устанавливается в соответствии с переданной схемой
> - `PrimOffset` и `RecordId` обнуляются
> - Счетчик длины блока данных сбрасывается

Это гарантирует, что следующие вызовы `EdfWriteData` будут начинать
новую последовательность записей с корректными заголовками.

---

### Блок данных (Data–block)

### Сериализация данных в бинарном виде

Содержит данные пользователя, сериализованные по конкретной схеме.
- Структуры данных рекурсивно и последовательно раскладываются на примитивы согласно схеме ([таблица-примитивных-типов](#Полная-таблица-примитивных-типов-PoType))
- Сериализация строго по схеме: Fields структуры, затем их вложенность или элементы массива, и т.д.
- Примитив не может быть разорван между блоками.
- Массив так же раскладывается на примитивы и элементы массива могут быть распределены между блоками.

Для всех структур которые планируете записывать строго использовать #pragma pack(push,1).
- **блока данных записи**: не может превышать Blocksize.
- **Struct**: ≤255 полей; всего ≤65535 примитивов включая вложенность.


#### Формат EdfRecordContent_t (бинарный):

| Поле      | Размер            | Описание                                                            |
|-----------|-------------------|---------------------------------------------------------------------|
| SchId     | 2 (uint16_t)      | Идентификатор схемы, к которой относятся данные                     |
| PrmOffset | 2 (uint16_t)      | Смещение по примитивам в записи (для восстановления)                |
| RecId     | 4 (uint32_t)      | Логический номер записи (RecordId)                                  |
| Data      | перем.            | Сериализованные значения (см. схему: порядок, типы, вложенность)    |
> Непосредственно перед данными размещены: SchId,PrmOffset,RecId - используются для восстановления последовательности данных после ошибок (например, пропуска или повреждения блока)


**Пример (записи данных для схемы SensorData):**
```
00 00       // SchId
00 00       // PrmOffset
00 00 00 00 // RecId
80 96 AD 65 // timestamp = 1700000000 (UInt32)
00 00 BC 41 // value = 23.5 (float)
02          // len = 2 (строка)
D0 C2       // '°','C'
```
---


### Сериализация данных в текстовый виде

- Представление:
  - `<~ ... >` — конфиг
  - `<? ... >` — схема
  - `<= {...}>` — данные
- Строки заключены в двойные кавычки, экранирование символов не поддерживается.
- Числа — в десятичном формате.
- Массивы, структуры — в фигурных скобках через `;` (row-major).

**Пример текстового файла:**
```
<~ {version=1.0; bs=512; encoding=65001; flags=0;} >

<? {0;"SensorData"} Struct {
  UInt32 "timestamp";
  Single "value";
  String "unit";
} >

<= {1700000000; 23.5;"°C";}
<= {1700000060; 23.6;"°C";}
```

---

## Десериализация

## Чтение множественных схем и записей

**В EDF-файле разрешается размещать сколько угодно блоков схем (Schema), каждый из которых отделяет партию блоков данных (Data), относящихся к именно этой схеме.**
После Schema идут блоки Data, **все они обязаны соответствовать последней объявленной схеме**; смешивать Data разных схем "вперемешку" нельзя, так как в кэше  хранится только последняя схема, т.е. между двумя блоками Schema все последующие блоки Data всегда принадлежат последней прочитанной схеме.

**Пример:**
```
<~ ... >
<? {0;"SensorData"} ... >
<= {...}>
<= {...}>
<? {1;"Pressure"} ... >
<= {...}>
<= {...}>
```

---

## API: функции работы с EDF

```c
// Инициализация
int EdfInit(EdfContext_t* ctx, uint8_t* mem, size_t memLen, const EdfConfig_t* cfg);
EdfContext_t* EdfCreate(uint8_t* mem, size_t memLen, const EdfConfig_t* cfg, int* pErr);

// Открытие потоков/файлов
int EdfOpenStream(EdfContext_t* ctx, Stream_t* stream, const char* mode);
int EdfOpenFile(EdfContext_t* ctx, const char* fname, const char* mode);

// Запись/чтение блоков, схем, данных
int EdfWriteConfig(EdfContext_t* ctx, size_t* writed);
int EdfWriteSchema(EdfContext_t* ctx, const EdfSchema_t* sch, size_t* writed);
int EdfWriteData(EdfContext_t* ctx, const void* data, size_t len);
int EdfFlushData(EdfContext_t* ctx, size_t* writed);

int EdfReadBlock(EdfContext_t* ctx);
int EdfReadBin(const EdfType_t* t, MemStream_t* src, LineAlloc_t* mem, void** presult, size_t* resultPrimOffset, size_t* primReaded);

int EdfClose(EdfContext_t* ctx);
```
- Для примитивов передавайте указатель на значение; для строк — на `const char*`.

---

## Коды ошибок

| Код     | Имя                   | Описание                   |
|---------|-----------------------|----------------------------|
| 0       | ERR_NO                | Успешно                    |
| -1      | ERR_EOF               | Конец файла                |
| 1001    | ERR_SRC_SHORT         | Недостаточно данных        |
| 1002    | ERR_DST_SHORT         | Недостаточно места         |
| 1003    | ERR_WRONG_TYPE        | Некорректный тип           |
| 1004    | ERR_WRONG_PARAMETERS  | Неверные параметры         |
| 1005    | ERR_FREAD             | Ошибка чтения файла        |
| 1006    | ERR_FWRITE            | Ошибка записи файла        |
| 1120    | ERR_BLK_WRONG_TYPE    | Неверный тип блока         |
| 1121    | ERR_BLK_WRONG_SIZE    | Неверный размер блока      |
| 1122    | ERR_BLK_WRONG_CRC     | Ошибка CRC                 |
| 1123    | ERR_BLOCK_SIZE_LARGE  | Превышение размера блока   |

---

## Примеры

```c
// Простейшая запись схемы и объекта
EdfConfig_t cfg = {0, 3, 65001, 512, 0, 0};
uint8_t mem[MEM_BLOCK_SIZE_512];
int err = 0;
EdfContext_t* ctx = EdfCreate(mem, sizeof(mem), &cfg, &err);

EdfSchema_t sch = {...}; // схема, см. выше по формату
EdfWriteConfig(ctx, NULL);
EdfWriteSchema(ctx, &sch, NULL);

MyRecord rec = {...};
EdfWriteData(ctx, &rec, sizeof(rec));
EdfClose(ctx);
```
*(Полные примеры см. тесты репозитория и секцию схемы.)*

---

## Практические ограничения, замечания

ещё не делал
- [ ] парсер (конвертер) из текстового представления в бинарный
- [ ] Сжатие целых чисел (опционально) - сложно, под вопросом:
Variable-length quantity - Wikipedia ,  Encoding | Protocol Buffers Documentation , Compact varint
- [ ] Разностное сжатие – сжатие данных, которые мало отличаются между измерениями + Variable-length, например время UNIX (int64 в секундах c 1970), время 1757582277(8 байт), 1757582278(8 байт) – 16 байт заменять на: 1757582277(8 байт), +1(1 байт) – 9 байт

> ## Недостатки
> * нужно или грузить в память целиком или обрабатывать в несколько проходов.
> * если нужна не вся информация из файла — все равно придется прочитать его весь или создавать индексы (ссылки на данные) отдельным файлом

> [!NOTE]
> ## использование конвертера **сedf.exe**
> * edf.exe 1D.D t – конвертер динамограммы в текстовый вариант 1D.tdf
> * edf.exe 1E.E t – конвертер эхограммы в текстовый вариант 1E.tdf
> * edf.exe 1DAT.DAT t – конвертер данных манометра (в т.ч. погружного) в текстовый вариант 1E.tdf
> * edf.exe 1D.D b – конвертер динамограммы в бинарный вариант 1D.bdf
> * edf.exe 1E.E b – конвертер эхограммы в бинарный вариант 1E.bdf
> * edf.exe 1DAT.DAT b – конвертер данных манометра (в т.ч. погружного) в бинарный вариант 1E.bdf
> * edf.exe 1D.bdf d – конвертер бинарного в данные эхограммы 1D.D
> * edf.exe 1E.bdf e – конвертер бинарного в данные эхограммы 1E.E
> * edf.exe 1DAT.bdf dat – конвертер бинарного в данные манометра 1DAT.DAT

### FAQ

**Q:** Можно ли смешивать записи разных схем вперемежку (по SchId)?
**A:** **Нет!** Data-блоки каждого типа идут строго после своей Schema (см. выше). Перемешивать Data для разных схем нельзя: схема не кэшируется. SchId в Data используется только для восстановления после потери блока.

**Q:** Как восстановить поток после потери блока?
**A:** Используйте SchId, PrmOffset, RecId блоков Data для правильного пропуска и восстановления последовательности при повреждении/потере блока.

**Q:** Что произойдёт, если схема или запись не помещается в блок?
**A:** Вернётся ошибка ERR_DST_SHORT или ERR_BLOCK_SIZE_LARGE.

---